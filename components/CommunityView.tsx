"use client";
import { useState } from "react";
import { ArrowUpRight, CircleDot, Users, X } from "lucide-react";
const rooms = [{ name: "Algorithms Lab", topic: "Dynamic Programming", members: 14, color: "bg-lilac" }, { name: "Physics Problem Room", topic: "Electromagnetism", members: 8, color: "bg-yellow" }, { name: "Exam Sprint", topic: "Calculus revision", members: 22, color: "bg-[#baf3ca]" }];
export function CommunityView() { const [room, setRoom] = useState<(typeof rooms)[number] | null>(null); const [inside, setInside] = useState(false); return <section className="view-enter mx-auto max-w-4xl px-4 pb-20 pt-36 sm:px-8 md:pt-28"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="font-display text-xs font-bold uppercase tracking-[.16em] text-muted">Prototype community</p><h1 className="mt-3 font-display text-3xl font-bold tracking-tight sm:text-5xl">Learn beside your peers.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-muted sm:text-base">A lightweight demo of study rooms and shared momentum. No accounts or real-time activity yet.</p></div><div className="flex items-center gap-2 rounded-full border border-ink bg-yellow px-4 py-2 font-display text-sm font-bold"><Users size={17} />42 learners active</div></div><div className="mt-8 grid gap-4 md:grid-cols-3">{rooms.map((item) => <article key={item.name} className="card-lift rounded-2xl border border-ink bg-white p-5 shadow-tactile"><div className={`flex h-11 w-11 items-center justify-center rounded-xl border border-ink transition-transform hover:rotate-3 ${item.color}`}><CircleDot size={19} /></div><p className="mt-5 font-display text-lg font-bold">{item.name}</p><p className="mt-1 text-sm text-muted">{item.topic}</p><p className="mt-4 text-xs font-semibold uppercase tracking-wider text-muted">{item.members} demo learners here</p><button type="button" onClick={() => { setRoom(item); setInside(false); }} className="tactile mt-4 inline-flex items-center gap-1 rounded-full border border-ink px-3 py-2 font-display text-sm font-semibold shadow-tactile-sm">Join room <ArrowUpRight size={15} /></button></article>)}</div><aside className="mt-6 rounded-2xl border border-ink bg-white p-5 shadow-tactile"><p className="font-display font-bold">Today’s demo activity</p><div className="mt-3 grid gap-2 text-sm text-muted sm:grid-cols-3"><span className="rounded-lg p-2 transition hover:bg-[#eeeeee]">⚡ 8 learners reviewing graphs</span><span className="rounded-lg p-2 transition hover:bg-[#eeeeee]">🧠 14 discussing DP</span><span className="rounded-lg p-2 transition hover:bg-[#eeeeee]">🎯 22 in revision sprint</span></div></aside>{room && <div className="fixed inset-0 z-[80] grid place-items-center bg-ink/30 p-4"><section className="w-full max-w-md animate-[rise_.2s_ease-out] rounded-2xl border border-ink bg-white p-5 shadow-tactile" role="dialog" aria-modal="true" aria-label="Demo study room"><div className="flex justify-between"><p className="font-display text-xs font-bold uppercase tracking-wider text-muted">Demo room</p><button onClick={() => setRoom(null)} aria-label="Close room" className="rounded-full p-1 hover:bg-[#eeeeee]"><X size={17} /></button></div>{inside ? <><h2 className="mt-2 font-display text-2xl font-bold">{room.name}</h2><p className="text-muted">{room.topic} · {room.members} demo learners</p><div className="mt-5 rounded-xl border border-ink bg-[#f3f3f3] p-4"><p className="font-display font-bold">Currently studying</p><p className="mt-1 text-sm">Dynamic Programming</p><p className="text-sm text-muted">Memoization vs Tabulation</p></div><div className="mt-4 space-y-2 text-sm text-muted"><p>Aarav reviewed recurrence trees</p><p>Maya opened the DP notes</p><p className="font-semibold text-ink">You joined the room</p></div><button onClick={() => setRoom(null)} className="tactile mt-5 w-full rounded-full border border-ink py-3 font-display font-semibold shadow-tactile-sm">Leave room</button></> : <><h2 className="mt-2 font-display text-2xl font-bold">{room.name}</h2><p className="mt-1 text-muted">{room.topic}</p><p className="mt-4 text-sm">{room.members} demo learners here</p><p className="mt-1 text-sm text-muted">You’re joining this demo room.</p><button onClick={() => setInside(true)} className="tactile mt-5 w-full rounded-full bg-ink py-3 font-display font-semibold text-white shadow-tactile">Enter room</button><button onClick={() => setRoom(null)} className="mt-2 w-full rounded-full py-2 font-display text-sm font-semibold text-muted hover:bg-[#eeeeee]">Cancel</button></>}</section></div>}</section>; }
