"use client";
import { ArrowRight } from "lucide-react";
export function CustomQuestion({ value, onChange, onSubmit }: { value: string; onChange: (value: string) => void; onSubmit?: () => void }) { return <section><label htmlFor="customQuestion" className="text-sm font-medium text-muted">Or tell StudyBud what you need…</label><div className="relative mt-1.5"><input id="customQuestion" value={value} onChange={(event) => onChange(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") onSubmit?.(); }} className="w-full rounded-xl border border-ink bg-white px-4 py-3 pr-14 text-sm outline-none shadow-tactile-sm placeholder:text-muted/70 transition focus:ring-2 focus:ring-ink" placeholder="e.g., Focus on memoization table vs recursion tree differences" /><button type="button" onClick={onSubmit} aria-label="Use custom question" className="tactile absolute right-2 top-1/2 grid h-9 w-9 -translate-y-1/2 place-items-center rounded-lg bg-ink text-white shadow-tactile-sm"><ArrowRight size={18} /></button></div></section>; }
