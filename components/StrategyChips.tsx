"use client";
import { strategies } from "@/lib/study";
export function StrategyChips({ value, onChange }: { value: string; onChange: (strategy: string) => void }) { return <section><div className="mb-2 flex items-center justify-between"><span className="font-display text-xs font-semibold uppercase tracking-wider text-muted">Choose a learning strategy</span><span className="hidden text-xs text-muted sm:block">Tailors tone &amp; depth</span></div><div className="flex flex-wrap gap-2">{strategies.map((strategy) => <button type="button" key={strategy} onClick={() => onChange(strategy)} className={`rounded-full border px-4 py-2 text-sm transition-all ${strategy === value ? "border-2 border-ink bg-[#eeeeee] font-bold shadow-tactile-sm" : "border-ink/30 bg-white font-medium hover:border-ink"}`}>{strategy}{strategy === "Explain simply" ? " 🧠" : strategy === "Exam-ready" ? " 🎯" : strategy === "Key concepts" ? " ⚡" : " 🪜"}</button>)}</div></section>; }
