"use client";
import { BookOpen, Brain, FileText, HelpCircle, Search } from "lucide-react";
import { StudyMode, modeLabels, modes } from "@/lib/study";
const icons = { explain: Brain, quiz: HelpCircle, summarize: FileText, research: Search, ask: BookOpen };
export function ModePicker({ value, onChange }: { value: StudyMode; onChange: (mode: StudyMode) => void }) { return <section><div className="mb-2 flex items-center justify-between"><h2 className="font-display text-lg font-bold">How should I help?</h2><span className="font-display text-[10px] font-bold uppercase tracking-wider text-muted">Select mode</span></div><div className="grid grid-cols-2 gap-2 sm:grid-cols-5">{modes.map((mode) => { const Icon = icons[mode]; const active = value === mode; return <button key={mode} type="button" onClick={() => onChange(mode)} className={`flex min-h-16 flex-col items-center justify-center gap-1 rounded-xl border px-2 py-2 font-display text-sm font-semibold transition-all sm:min-h-11 sm:flex-row ${mode === "ask" ? "col-span-2 sm:col-span-1" : ""} ${active ? "border-ink bg-ink text-white shadow-tactile" : "border-ink/40 bg-white hover:border-ink hover:bg-[#f3f3f3]"}`}><Icon size={17} />{modeLabels[mode]}</button>; })}</div></section>; }
