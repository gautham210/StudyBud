"use client";
import { useEffect } from "react";
import { X } from "lucide-react";
import { DemoStreak } from "@/lib/demoStorage";
export function StreakPopover({ open, streak, onClose }: { open: boolean; streak: DemoStreak; onClose: () => void }) { useEffect(() => { const close = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); }; if (open) window.addEventListener("keydown", close); return () => window.removeEventListener("keydown", close); }, [open, onClose]); if (!open) return null; const days = ["M", "T", "W", "T", "F", "S", "S"]; return <section className="absolute right-0 top-12 z-[70] w-72 animate-[rise_.16s_ease-out] rounded-2xl border border-ink bg-white p-4 shadow-tactile" role="dialog" aria-label="Study streak"><div className="flex items-start justify-between"><div><p className="font-display text-lg font-bold">🔥 {streak.days} day streak</p><p className="mt-1 text-sm text-muted">Keep learning to grow your streak.</p></div><button type="button" aria-label="Close streak" onClick={onClose} className="rounded-full p-1 hover:bg-[#eeeeee]"><X size={16} /></button></div><div className="mt-4 grid grid-cols-7 gap-1.5">{days.map((day, index) => <div key={`${day}-${index}`} className="text-center"><span className={`grid h-7 w-7 place-items-center rounded-full text-xs font-bold ${streak.completedDays.includes(index) ? "bg-ink text-white" : index === 5 ? "border border-ink bg-yellow" : "bg-[#eeeeee] text-muted"}`}>{day}</span></div>)}</div><p className="mt-3 text-[10px] font-semibold uppercase tracking-wider text-muted">Demo progress saved on this device</p></section>; }
