import { Sparkles } from "lucide-react";
export function EmptyOutput({ submitted = false }: { submitted?: boolean }) { if (submitted) return null; return <section className="rounded-2xl border border-dashed border-ink/40 bg-white/60 p-7 text-center sm:p-9"><div className="mx-auto grid h-11 w-11 place-items-center rounded-xl border border-ink bg-yellow"><Sparkles size={20} /></div><h2 className="mt-4 font-display text-lg font-bold">Your learning space will appear here</h2><p className="mx-auto mt-1 max-w-md text-sm leading-6 text-muted">Choose a source and a learning mode, then generate a study space tailored to how you learn best.</p></section>; }
